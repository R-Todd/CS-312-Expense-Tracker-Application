# =========================================================
# Server Configuration
# =========================================================
SERVER_PORT=5000


# =========================================================
# PostgreSQL Container Credentials
# =========================================================
# These values configure the Postgres database inside Docker.
# Replace with your own credentials before running the server.
POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password
POSTGRES_DB=your_database_name


# =========================================================
# Server Database Configuration
# =========================================================
# The host must stay "db" because it refers to the Docker
# service name in docker-compose.yml.
DB_HOST=db
DB_PORT=5432

# Automatically reuse the values defined above
DB_NAME=${POSTGRES_DB}
DB_USER=${POSTGRES_USER}
DB_PASSWORD=${POSTGRES_PASSWORD}

# =========================================================
# JWT Secret Key
# =========================================================
JWT_SECRET= your_jwt_secret_key_here # long random string
